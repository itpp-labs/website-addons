<?xml version="1.0" encoding="utf-8"?>
<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="valuta.list">
      <field name="name">Valuta list</field>
      <field name="model">valuta.valuta</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="valuta.action_window">
      <field name="name">Valuta window</field>
      <field name="res_model">valuta.valuta</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="valuta.action_server">
      <field name="name">Valuta server</field>
      <field name="model_id" ref="model_valuta_valuta"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->
  
    <!-- Top menu item -->
    <!--
    <menuitem name="Valuta" id="valuta.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="valuta.menu_1" parent="valuta.menu_root"/>
    <menuitem name="Menu 2" id="valuta.menu_2" parent="valuta.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="valuta.menu_1_list" parent="valuta.menu_1"
              action="valuta.action_window"/>
    <menuitem name="Server to list" id="valuta" parent="valuta.menu_2"
              action="valuta.action_server"/>
    -->
  </data>
</openerp>