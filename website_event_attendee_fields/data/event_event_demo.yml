-
  !python {model: event.event}: |
    self.search([]).write({
        'attendee_fields': [(6,0,[
            self.env.ref('website_event_attendee_fields.attendee_field_name').id,
        ])]
    })
 
